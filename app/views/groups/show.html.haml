%h1
  Stream for
  = link_to @group.name, @group

= render "shared/publisher", :group_id => @group.id
%ul#stream
  - for post in @posts
    = render type_partial(post), :post => post
#pagination
  = will_paginate @posts

