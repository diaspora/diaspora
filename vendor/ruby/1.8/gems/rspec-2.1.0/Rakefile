require "bundler"
Bundler.setup
Bundler::GemHelper.install_tasks

require 'rake'
require 'fileutils'
require 'pathname'

task :clobber do
  rm_rf 'pkg'
end

task :default do
  puts "Nothing to do for the default task"
end

